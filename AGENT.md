# InkStack Agent 指令

本文件是 `inkstack-(墨栈)` 的项目级常驻指令。后续 AI / Agent 开发本项目时，必须先阅读本文件，再结合当前用户任务执行。

本项目是对旧项目 `/Users/zhangzehua/Downloads/InkStack` 的重新搭建。旧项目只能作为产品能力、交互经验和局部纯逻辑的参考来源；本项目的技术栈、目录结构和代码风格以当前仓库为准。

长期产品能力、AI 原生编辑器设想、竞品参考和桌面化整改路线，见 `桌面AI原生Markdown编辑器整改计划.md`。本文件只保留高频硬约束和开发边界。

---

## 1. 项目定位

InkStack 是本地优先的桌面 Markdown 写作、阅读和 AI 辅助 App。目标形态是 macOS 上可长期日用的本地应用，而不是网页产品。

核心目标：

- 保持 Markdown 原生 `.md` / `.markdown` 文件格式。
- 能作为系统默认 Markdown 编辑器打开文件。
- 支持桌面日常使用场景：双击文件打开、拖拽文件/目录、最近文件、菜单栏、快捷键、窗口状态恢复、系统深浅色模式。
- 提供稳定的目录打开、文件树、编辑、保存、预览和大纲能力。
- 在不破坏本地隐私和用户确认流程的前提下，逐步接入 AI 辅助写作。
- 代码结构要严谨、模块边界清楚，并在关键复杂逻辑处保留维护性注释。

不要把 InkStack 做成浏览器网页、在线编辑器或只能展示单篇文本的网页 Demo，也不要引入私有文档格式。React/Vite 只是前端界面技术；产品和架构判断必须按桌面 App 处理。

---

## 2. 当前技术栈优先级

当前仓库已经采用以下栈，后续开发默认沿用：

```text
前端框架：React 19 + TypeScript
构建工具：Vite
桌面壳：优先 Tauri 2
桌面后端：优先 Rust command / plugin / secure storage
状态管理：Zustand
编辑器：CodeMirror 6 / @uiw/react-codemirror
Markdown 预览：react-markdown + remark / rehype
数学公式：KaTeX
图表渲染：Mermaid
代码高亮：highlight.js
样式系统：Tailwind CSS 4 + CSS variables
图标：lucide-react
AI 接入：可配置 provider 层；桌面正式版应由 Tauri/Rust 后端或安全存储承载密钥
```

旧项目是 SvelteKit + Tauri 2 + Rust。除非用户明确要求恢复 Svelte，否则不要为了复用旧项目而把当前 React/Vite 项目改回 Svelte。桌面能力不是可选项：需要逐步在当前 React 前端外接 Tauri 2，而不是长期依赖浏览器能力。

---

## 3. 旧项目复用判断

开发前可参考旧项目 `/Users/zhangzehua/Downloads/InkStack`，但必须按下面规则判断是否复用。

### 3.1 优先复用的内容

这些内容可以迁移其设计、数据结构或纯函数思想：

- Markdown 原生文件的产品约束。
- 工作区目录树、Markdown 文件过滤、隐藏文件忽略、目录优先排序。
- 大目录扫描的限制、懒加载和异步处理思路。
- 文件元数据基线：保存前检查 `modified_at` / `size`，避免覆盖外部修改。
- 自动保存的防抖机制和 `idle` / `dirty` / `saving` / `saved` / `error` 保存状态。
- 最近工作区、打开标签页、活动文件、窗口尺寸、写作设置等持久化设置模型。
- 命令面板、快捷打开、布局切换、全文搜索、大纲面板等交互能力。
- Markdown 预览增强能力：front matter 忽略、目录占位符、脚注、任务列表、Mermaid、KaTeX、代码高亮、相对图片路径解析。
- 主题系统的概念：内置写作主题、自定义主题 manifest、CSS variables。
- AI 安全边界：上下文最小化、修改前预览或 diff、用户确认后写入。

### 3.2 谨慎复用的内容

这些内容只能在适配当前 React/TypeScript 结构后使用：

- 旧项目 `src/lib/types.ts` 中的类型定义。
- 旧项目 `src/lib/commands.ts` 的命令组织方式。
- 旧项目 `src/lib/outline.ts` 的 Markdown 大纲解析思路。
- 旧项目 `src/lib/writing-themes.ts` 的主题变量生成逻辑。
- 旧项目 `src-tauri/src/commands.rs` 中的文件系统安全策略。

迁移时要重命名、重分层、补类型，不允许把 Svelte/Tauri 绑定代码硬塞进 React 组件。

### 3.3 不应复用的内容

以下内容不要直接迁移：

- `.svelte` 组件和 Svelte `$state` / `$derived` 写法。
- `.svelte-kit/`、`build/`、`node_modules/`、`target/` 等生成物。
- 旧项目中的占位组件，除非先转化为本项目真实功能。
- 与当前产品方向不一致的 UI 布局、样式细节或临时实现。
- 会暴露 API Key、绕过用户确认或默认上传整个文档库给 AI 的实现。

---

## 4. 当前开发优先级

优先完成稳定、可维护的 MVP，再扩展高级能力。

第一阶段优先级：

1. 接入 Tauri 桌面壳，并支持 macOS 作为 `.md` / `.markdown` 默认打开应用。
2. 打开本地目录或文件。
3. 展示 Markdown 文件树。
4. 打开、编辑并保存 Markdown。
5. 自动保存和外部修改保护。
6. 实时预览，预览渲染必须防抖。
7. 分屏、编辑、阅读布局切换。
8. 深色 / 浅色模式。
9. 大纲、标签页、最近工作区。
10. 搜索、命令面板、主题系统。
11. AI 辅助写作，但必须带确认流程。

不要在基础文件流尚未稳定时提前堆叠插件市场、双链图谱、复杂导出、云同步等高级能力。

---

## 5. 代码结构要求

当前结构可以继续演进，但新增代码要按职责分层：

```text
src/
  components/       UI 组件，尽量保持展示和交互清晰
  lib/              与框架弱绑定的工具、解析、文件、AI provider
  store.ts          全局状态入口；复杂状态可拆成 slice 或独立模块
  App.tsx           应用布局组合，不承载重业务逻辑
```

当功能变复杂时，优先拆分为：

```text
src/features/editor/
src/features/preview/
src/features/workspace/
src/features/ai/
src/features/settings/
```

开发规则：

- 组件只处理 UI 和局部交互，文件系统、AI、Markdown 解析等重逻辑放到 `src/lib` 或 feature service。
- Zustand store 不要变成杂物箱；跨组件共享的状态才进入 store。
- 类型定义要集中、明确，避免在多个组件中重复声明相似结构。
- 异步操作要有 loading / error / stale request 处理，避免旧请求覆盖新状态。
- 大块复杂逻辑前应写简短注释说明“为什么这样做”，不要写无意义注释。
- UI 文案默认中文优先；如保留中英切换，文案要统一管理。

---

## 6. 文件系统与数据安全

- Markdown 文件必须保持原生格式。
- 不允许默认读取或上传整个目录内容给 AI。
- 文件保存前要尽量保留外部修改检测能力，避免覆盖用户在其他编辑器中的改动。
- 目录扫描必须跳过隐藏文件、`node_modules`、构建产物和大型缓存目录。
- 大目录扫描要异步、分批或懒加载，不能阻塞编辑输入。
- 文件系统正式实现优先走 Tauri/Rust 后端 API。浏览器 File System Access API 只能作为早期原型或临时开发路径，不应成为长期架构。
- API Key 不允许写入前端源码，不允许明文持久化。桌面正式版应通过 Tauri secure storage、系统钥匙串或 Rust 后端代理承载密钥。
- 不要用“浏览器限制”作为产品设计前提；遇到文件访问、密钥安全、系统集成问题时，优先考虑桌面 App 能力。

---

## 7. Markdown 预览要求

预览能力要以安全和性能为先：

- 每次编辑触发预览更新必须防抖。
- 对用户 Markdown 中的 HTML 必须显式评估安全策略；如启用 `rehype-raw`，必须搭配 sanitize。
- Mermaid、KaTeX、代码高亮等重渲染要缓存或延迟处理，避免输入卡顿。
- 相对图片路径解析应以当前文档路径为基准，并限制在用户授权目录内。
- 预览功能优先保证 GFM、表格、任务列表、代码块、数学公式、Mermaid 可用。

---

## 8. AI 功能硬约束

AI 是辅助写作能力，不是默认接管编辑器的自动改写器。

- 发送给 AI 的上下文必须最小化，默认只使用当前文档或用户明确选中的片段。
- AI 生成的改写不能直接覆盖正文，必须先展示预览、diff 或可撤销变更，并由用户确认。
- 用户未授权时，不得批量读取工作区内容作为上下文。
- AI provider 要抽象成可替换模块，避免组件直接依赖具体模型 SDK。
- 错误提示要清楚说明是网络、密钥、额度、模型还是解析问题。

当前 AI provider 层支持用户在 UI 中配置国内外厂商。开发原型可以临时使用前端 `fetch` 验证协议，但桌面正式方案必须迁移到 Tauri/Rust 后端或安全存储路径，避免把 API Key 暴露在前端运行环境中。

---

## 9. UI 与体验准则

- InkStack 是写作工具，界面要克制、稳定、适合长时间编辑。
- 不要做营销式首页；启动后的第一屏应是可用的编辑工作台。
- UI 与交互按桌面生产力 App 设计，优先考虑菜单栏、系统快捷键、文件关联、拖拽、窗口恢复、原生文件选择器和系统通知。
- 工具按钮优先使用 `lucide-react` 图标，并提供 `title` 或可访问标签。
- 布局要避免嵌套卡片和过度装饰，优先清晰的信息层级。
- 编辑器、预览区、侧边栏、AI 面板、状态栏的尺寸变化不能造成明显跳动。
- 移动端不是第一优先级，但基础响应式不能出现文字重叠或控件溢出。

---

## 10. 验证要求

修改后根据影响范围执行验证：

- 类型和基础构建：`npm run lint`、`npm run build`。
- UI 变更：开发阶段可用 `npm run dev` 检查界面；涉及桌面能力时必须用 Tauri dev/build 或等效桌面运行路径验证。
- 文件功能：至少验证打开、读取、编辑、保存、保存失败提示。
- AI 功能：验证无密钥、错误密钥、正常响应、取消/确认改写路径。

如果因为环境限制无法运行验证，必须在最终回复中说明未验证项和原因。

---

## 11. 文档冲突优先级

如果后续出现文档或代码约束冲突，按以下顺序处理：

1. 用户当前明确要求。
2. 本 `AGENT.md` 的硬约束。
3. 当前代码实际技术栈和可维护性。
4. 旧项目中已验证有效的设计经验。
5. 长期设想或临时 Demo 代码。

无法判断时，先说明冲突点，再给出建议方案。
